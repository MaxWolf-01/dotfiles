#!/usr/bin/env bash
# Size of each given file or directory (apparent size, human-readable).
# With no arguments: every entry in the current directory, hidden ones included.
# Usage: fs [path...]
set -euo pipefail
shopt -s nullglob dotglob
(( $# )) || set -- *
du -sbh -- "$@"
