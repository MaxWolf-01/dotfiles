#!/usr/bin/env bash
# The n largest entries under the current directory (default 10); a negative
# n lists the n smallest instead.
# Usage: lsfs [n]
set -eu
n=${1:-10}
if (( n < 0 )); then
  du -ah | sort -h | head -n $(( -n ))
else
  du -ah | sort -hr | head -n "$n"
fi
