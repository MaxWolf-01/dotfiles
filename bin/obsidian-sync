#!/bin/bash

# Main repo
cd ~/repos/obsidian/knowledge-base
git add .
git commit -a -m "Vault backup: `date +'%Y-%m-%d %H:%M:%S'`"
git pull --no-ff
git push

# Settings
cd .obsidian
git add .
git commit -a -m "Obsidian settings backup: `date +'%Y-%m-%d %H:%M:%S'`"
git pull --no-ff
git push

