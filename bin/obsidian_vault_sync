#!/bin/bash
cd /storage/emulated/0/obsidian/knowledge-base || exit
git pull --ff-only || echo "ff-only not possible -> stash, pull, pop to resolve locally OR use <push_conflicts> alias to deal with it on another machine" && exit
git add .
git commit -m -m "Android vault backup: `date +'%Y-%m-%d %H-%M-%S'`"
git push
