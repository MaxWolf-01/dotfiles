- on a branch: replace noctalia with waybar+mako+hyprlock+hypridle on xmg19, style nicely, make screenshots/recording work
- keybind-cheatsheet plugin for noctalia/hyprland: https://github.com/noctalia-dev/noctalia-plugins/tree/main/keybind-cheatsheet
- clipboard history on GNOME 50: blocked until GPaste or Clipboard Indicator update for GNOME 50. clipse/cliphist trigger dock auto-show (wl-clipboard bug). Works on Hyprland with clipse.
- tiling-shell on GNOME 50: v71 only supports up to GNOME 49. Currently runs as "OUT OF DATE". Monitor upstream for GNOME 50 support.
- test hibernation on xmg19 AND zephyrus (swap + resumeDevice configured on xmg19, zephyrus needs swap first). NVIDIA suspend/resume with PreserveVideoMemoryAllocations.
- zephyrus NixOS: enable hardware.nvidia.dynamicBoost (RTX 3070 Ti supports it, shifts power between CPU+GPU dynamically)
- screen mirroring/casting support in NixOS desktop config (PipeWire + xdg-desktop-portal, needed for Discord etc.)
- xmg19 nixos + containerized skip perms agent fleet setup (see agent/tasks/2026-03-13-xmg19-agent-workstation.md)
- Build custom NixOS installer ISO with SSH keys + networking baked in (config in flake as nixosConfigurations.installer, just needs `nix build`)
- Try https://github.com/fleek-sh/nix2gpu for reproducible GPU containers on RunPod/vast.ai
- udiskie for auto-mounting removable drives (udisks2 enabled, udiskie not yet added)
- **https://github.com/kiyoon/jupynium.nvim** for labs...
- fix: separate .obsidian repos or better handling of platform specific differences
- vault-sync: remove MAX_FILE_SIZE limit once onyx.md launches and site is self-hosted (Hetzner). Currently skips files >100MB due to GitHub's limit.
- WakaTime for Vesktop: HM has no userplugin support, userplugins require building Vencord from source.
  Options: Nix overlay (complex), fork vesktop (maintenance burden), or skip Nix for this—use upstream Vesktop AppImage + build locally with wakatime in src/userplugins/.
  https://github.com/wakatime/vencord-wakatime

Claude idea:
- As for the global claude knowledge, the way I could utilize or like populate it (core question) is by distilling from all other agent/ knowledge bases on my pc...?

old prmpt improvemnts sess for mx distill, delegation, rost, but prlly not worth it? not rn
claude --resume 9cc79e54-367b-4511-aa15-22298597e60f
