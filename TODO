Shell doesn't fully load / hang if no internet? Or rather if slow internet. Due to uv?


- Obsidian (+refactor dependent aliases/funcs like archive_md): think about a better concept for archived web stuff + papers 

- refactor: remove dotbot dep; simple link/... script (or just put it all in install.sh
  - restructure theming / plugins / general dotfiles strucutre to be a bit more straightforward and intuitive?
- fix: android nvim setup or just dont alias vim to nvim
- fix: other platform secific (android) fixes: source android zshr gitconfig [safe]
- fix: separate .obsidian repos or better handling of platform specific differences

- claude idea: "Last words" command, that asks the model whether it still has anything relevant that only it knows (global knowledge, project knowledge, unfinished work, ...) that it wants to write down b4 i say bye forever :(

